# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Include other route files
-> 			/ 			auth.Routes
-> 			/ 			user.Routes
-> 			/ 			search.Routes
-> 			/ 			bucket.Routes
-> 			/ 			shopplan.Routes
-> 			/ 			feed.Routes